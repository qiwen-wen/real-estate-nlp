# Dependencies for the Streamlit-Cloud-only demo deploy.
# The UI does the actual ML work in-process (no separate FastAPI service),
# so this file mirrors the production ML stack — minus the API-only bits
# (fastapi, uvicorn, slowapi, httpx, mysql-connector, jupyter, pytest, etc.).
streamlit>=1.31.0
pandas>=2.1.0
numpy>=1.26.0
nltk>=3.8.1
scikit-learn>=1.3.0
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0
rank_bm25>=0.2.2
